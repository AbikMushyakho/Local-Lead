import 'package:flutter/material.dart';

class GuideProfilePageScreen extends StatelessWidget {
  const GuideProfilePageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GuideProfilePageScreen'),
      ),
    );
  }
}